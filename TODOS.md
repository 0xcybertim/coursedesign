# Product and Design TODOs

## User-created Profile Wing workflow

### Verify one real user-owned image before treating the provider seam as signed off

- **Status:** Implementation complete on 2026-07-23. `/studio/obstacles/profile-wing/new` now accepts a PNG/JPEG upload or the currently accepted Concept Studio artifact, creates a metadata-stripped browser-local derivative, and makes no provider request until the user confirms rights, remove.bg processing, no identifiable person, and one clear subject.
- **Processing contract:** The local-only server route holds the remove.bg credential, validates host, origin, byte limit, magic bytes, SHA-256, and confirmations, permits one active request per browser session, times out after 60 seconds, and performs zero automatic retries. A successful transparent PNG is stored by SHA-256, its alpha is passed through the existing deterministic vectorizer/validator, and source, cutout, and polygon are shown side by side for mandatory visual acceptance.
- **Decision and product flow:** `Accept & build Profile Wing` creates the exact shared 2.5D/3D geometry; `Retain without conversion` preserves evidence without creating geometry; another provider call requires a new explicit click. Accepted geometry appends an immutable generated-prototype revision to the v2 design library and can be placed in the existing course and review flows with generated/inferred provenance.
- **Quality evidence:** `pnpm format:check`, `pnpm lint`, `pnpm typecheck`, `pnpm test` (227 tests), and `pnpm build` pass. Production-browser acceptance passes 10 checks across upload, confirmations, one deterministic local request, mask/polygon review, visual acceptance, 2.5D/3D parity, immutable save, course placement/review, 1440/768/375 px layouts, and clean browser diagnostics. The QA run made zero live remove.bg calls and zero external browser requests.
- **Evidence:** `docs/profile-wing-creation/IMPLEMENTATION.md`, `docs/profile-wing-creation/COMPLETION_AUDIT.md`, `docs/profile-wing-creation/browser-acceptance.json`, and seven inspected screenshots under `docs/profile-wing-creation/screenshots/`.
- **Pending real seam:** Rotate the remove.bg key that was pasted into chat, store the replacement only in ignored `.env.local`, and have Tim process and visually accept one representative user-owned image. That explicit live action may consume provider quota and remains the final provider/image sign-off.
- **Boundary:** Browser-local, non-sellable prototype only. No identifiable people, accounts, cloud persistence, cross-device sync, sharing, supplier approval, fabrication or structural validation, horse-safety approval, pricing, checkout, payment, or ordering.

## Phase 1G developer-only concept generation

### Verify the real provider before any broader concept-to-product work

- **Status:** Phase 1G mechanics and the honest Workflow simulator usability goal are complete. Bounded live OpenAI generation passed earlier. On 2026-07-21, one newly authorized edit-only follow-up returned four genuine refinements with zero generation calls and no retries. Dog silhouettes, ears, palette, gate, and sponsor areas visibly changed or survived, but all four outputs showed three horizontal poles instead of the authoritative four; the structural-fidelity gate therefore remains failed. Tim subsequently said “Perfect.”; this is recorded as approval of the images as creative-only source material, not approval of their obstacle structure.
- **Implemented:** `/studio/concepts/new` now treats family, silhouette subject, four locked poles, colors, lower element, sponsor area, and style as authoritative. `Creative direction — optional` is limited to extra pose/expression/marking/composition detail, and a readable Generation brief shows the exact combined direction before submission. A bounded common-subject check blocks obvious contradictions with an associated accessible warning and keyboard-operable one-click resolution; the server repeats validation.
- **Simulator truth:** Deterministic mode is named `Workflow simulator`, states that it makes no live calls and cannot demonstrate AI image quality, and produces labelled zero-cost fixtures. Dog, butterfly, castle/building, horse, leaf, and wave have distinct bounded shapes; known colors affect fills; style, lower element, sponsor area, variant, and refinement direction are visible; unknown allowed subjects use an honest generic fallback. Simulator photo mode tests preprocessing and workflow only and explicitly does not claim genuine matching.
- **Storage and product boundary:** Processed reference and generated bytes reuse the Phase 1F SHA-256-verified IndexedDB store; metadata stores hashes/IDs only; temporary object URLs are revoked. Selecting and `Build this concept` record only the Phase 1H handoff. No SPJ-04 configuration/revision, course quantity, geometry, quote, or supplier-approved record is created or mutated.
- **Quality evidence:** `pnpm format:check`, `pnpm lint`, `pnpm typecheck`, `pnpm test` (165 tests), `pnpm build`, and the six-case zero-call offline evaluation pass. Exact-production browser acceptance passes 24 checks across conflict resolution, dog/butterfly/castle fixtures, known colors and remaining control effects, four-result batches, immutable regeneration/refinement, photo simulation, cancellation, five-batch reload, missing bytes, selection boundary, Phase 1F isolation, responsive containment, focus/announcements, 44 px targets, object-URL cleanup, zero console warnings/errors, zero unexpected failed requests, and zero live OpenAI calls.
- **Evidence:** `docs/phase-1g/IMPLEMENTATION.md`, `docs/phase-1g/COMPLETION_AUDIT.md`, `docs/phase-1g/browser-acceptance.json`, the original `docs/phase-1g/live-provider-acceptance.json`, the edit-only `docs/phase-1g/live-refinement-followup.json`, and five simulator screenshots under `docs/phase-1g/screenshots/`. Codex inspected the live follow-up outputs; human visual sign-off is explicitly deferred.
- **Live-provider status:** The original low-quality generation returned HTTP 200 with four JPEGs; its one edit attempt returned HTTP 429. The separately authorized 2026-07-21 follow-up made zero generation calls and exactly one low-quality edit request against a local deterministic dog fixture. It returned HTTP 200 with four 1,024 px JPEGs on `gpt-image-2-2026-04-21`, with `maxRetries: 0`. The images genuinely changed the dog silhouette and preserved most requested visual attributes, but every output violated the locked four-pole direction by rendering three horizontal poles. Evidence records hashes, counts, dimensions, model, and request provenance only; the temporary credential was deleted immediately after the call.
- **Boundary:** No Phase 1H silhouette extraction/vectorization, generated obstacle geometry, product revisions from concepts, public deployment, accounts/cloud sync/sharing, supplier/fabrication/safety claims, pricing, checkout, payment, or ordering.
- **Exact next gate:** Phase 1H-C1 is complete. The pure domain derivation validates the current B2 decision, fits one shared integer-mm polygon, emits two mirrored plate instances, four fixed poles, inferred supports, generic counts, a controlled footprint, and exact 2.5D/3D geometry identity. Phase 1H-C2 must be separately scoped as a read-only visible renderer-parity proof with WebGL fallback and no revisions or storage migration.

## Phase 1H-A dedicated subject-mask provider benchmark

### Prove a trustworthy raster mask before any silhouette conversion

- **Status:** Complete. The bounded Photoroom sandbox benchmark failed because all outputs were watermarked. The separately authorized remove.bg free-preview benchmark then passed: 20/20 clean masks accepted, 9/9 unsafe cases rejected, all returned masks visually inspected, exactly 29 calls, zero retries, and $0 cost. The separately bounded Phase 1H-B1 local vectorization kernel is also complete; no Phase 1H product implementation has begun.
- **Provider finding:** Photoroom returned direct grayscale alpha masks with uncertainty and simple stateless JavaScript integration, but its free sandbox watermarked every saved output. Tim accepted the synthetic-only privacy/retention terms before the completed run. Sandbox output is integration evidence only and is not eligible for vectorization; regular-tier quality remains unproven.
- **Finite remove.bg run:** Exactly 20 clean fixtures, 3 empty, 3 multi-subject, and 3 badly occluded; 29 requests, 26 HTTP 200 masks, 3 explicit HTTP 400 empty-scene rejections, one attempt per fixture, zero retries, 521 ms median, 1,201 ms p90, 3,114 ms maximum, and $0 cost.
- **Evidence:** `docs/phase-1h/REMOVE_BG_PROVIDER_SETUP.md`, `REMOVE_BG_BENCHMARK_REPORT.md`, `REMOVE_BG_COMPLETION_AUDIT.md`, `remove-bg-live-benchmark-authorization.json`, `remove-bg-live-benchmark-results.json`, 26 masks under `remove-bg-live-masks/`, plus the historical Photoroom evidence, manifest, local validation, tools, and evaluation tests.
- **Boundary:** No contour tracing, vectorization, validation geometry, profile-wing product definition, obstacle generation, 2.5D/3D product rendering, revision/course integration, migration, OpenAI call, accounts, deployment, commerce, fabrication, safety, or supplier approval.
- **Exact next gate:** Phase 1H-B1/B2 and C1 now prove deterministic conversion, the human decision seam, and a renderer-neutral inferred profile-family definition. Do not start automatically: Phase 1H-C2 requires a separate visible SVG/Three.js parity scope without allowing the prototype to become a saved product revision.

## Phase 1F artwork foundation and logo customization

### Add content-addressed artwork to the bounded SPJ-04 workflow

- **Status:** Complete for the browser-local Phase 1F prototype on 2026-07-15; native operating-system file-picker observation, representative-device performance, visual sign-off, and supplier print confirmation remain pending
- **Implemented:** SPJ-04 accepts byte-detected PNG/JPEG and sanitized SVG, produces a canonical PNG up to 2,048 px, stores source and render bytes by SHA-256 in IndexedDB, and keeps only hash-addressed metadata in the local draft/revision graph. Typed failures cover unsupported PDF, spoofing, unsafe/complex SVG, corrupt decode, size/dimension limits, missing/corrupt storage, capacity, and quota conditions.
- **Placement and parity:** Linked wings are the safe default; independent left/right placement supports contain/cover, integer scale/position/rotation, explicit backgrounds, alignment shortcuts, and prototype bleed/safe-area guides. The exact manifest hashes and normalized placement records drive both deterministic 2.5D panels and the named left/right Three.js artwork meshes.
- **Persistence and history:** Confirm stores and re-verifies blobs before draft metadata changes. Immutable save refuses unavailable or corrupt artifacts. Reload resolves the same content hashes; replacement/removal never mutates older revisions; course placements and the Course Review Sheet continue pinning exact revision IDs and configuration hashes. Missing local artwork stays visible as incomplete instead of being substituted.
- **Quality evidence:** `pnpm format:check`, `pnpm lint`, `pnpm typecheck`, `pnpm test` (133 tests), and `pnpm build` pass. Production-browser acceptance passed 20 checks covering PNG/JPEG/SVG and typed rejection, independent artwork, exact 2.5D/3D parity, two immutable revisions, reload/read-only identity, course/review pinning, forced WebGL fallback, keyboard focus, 44 px targets, 375/768/1440 px containment, object-URL cleanup, zero failed requests, and zero application warning/error console output.
- **Evidence:** `docs/phase-1f/IMPLEMENTATION.md`, `docs/phase-1f/browser-acceptance.json`, and nine screenshots under `docs/phase-1f/screenshots/`.
- **Boundary:** Browser-local prototype only. No account/cloud persistence, cross-device sync, sharing, automatic revision migration, generative concepts, silhouette conversion, supplier-approved print rules, production-ready artwork, safety/fabrication approval, pricing, checkout, payment, or ordering.
- **Exact next gate:** Tim's visual sign-off, a real representative-device performance pass, native file-picker observation on an unlocked desktop, and supplier confirmation of print area/bleed/safe area/fastener/material/color/format rules. Phase 1G concept generation must begin only as a separately approved milestone; Phase 1H silhouette conversion remains out of scope.

## Phase 1E explicit course revision updates

### Add preview, cancel, and atomic confirmation over immutable revisions

- **Status:** Complete for the bounded browser-local Phase 1E prototype on 2026-07-14; visual sign-off, representative-device performance, supplier confirmation, authenticated persistence, and commercial workflows remain pending
- **Implemented:** The Course Review Sheet now offers explicit `Update this placement` and exact-source `Replace all using this revision` actions. Both open an accessible deterministic preview with exact targets, revision IDs/ordinals, before/after quantities and deltas, geometry warnings, hashes, typed validation, and non-sellable boundary copy before any write.
- **Confirmation contract:** Confirmation re-reads browser storage, validates source/destination compatibility and exact stale preconditions, constructs one complete validated replacement course, increments the draft version once, updates the timestamp once, performs one course-key localStorage write, and proves the resulting review hash matches the preview. Immutable obstacle revisions and unrelated placements are unchanged.
- **Quality evidence:** `pnpm format:check`, `pnpm lint`, `pnpm typecheck`, `pnpm test` (99 tests), and `pnpm build` pass. Production-build browser acceptance covered cancel/close/Escape safety, update-one, exact replace-all, reload persistence, one-write atomicity, stale-preview rejection, visible/JSON parity, keyboard focus and announcements, clipboard behavior, desktop/tablet/mobile containment, 44 px targets, print-hidden controls, normal 3D, forced 2.5D fallback, and an empty final warning/error console.
- **Hashes:** Initial `5bfd119dc39ea9f449ec2e8427300a0433925f921a0da6da651f26c0b3ade393`; update-one `cb234425e3d07ee09547020b2d8e1c225cfea313139ba0254a92aa8aac36add3`; exact replace-all `fcf7087dc4dcfd57b3de7134a647c1932c0c078eb791125101d3de124ce04d8f`.
- **Screenshots:** `docs/phase-1e/screenshots/update-one-preview.png`, `update-one-confirmed.png`, `replace-all-preview.png`, `replace-all-confirmed-review.png`, and `mobile-update-flow.png`.
- **Boundary:** No automatic upgrades or downgrades, revision mutation, accounts, server persistence, cross-device sync, sharing, recovery/history, supplier approval, fabrication/safety/course certification, pricing, checkout, payment, ordering, or production release.
- **Exact next gate:** Tim's visual sign-off, representative-device performance validation, and supplier confirmation. Do not automatically begin server-backed or commercial implementation; Phase 2 commerce still requires its separate architecture and business-truth gates.

## Phase 1D course review and production-spec preview

### Add the deterministic read-only review boundary

- **Status:** Complete for the bounded browser-local Phase 1D prototype on 2026-07-13; authenticated, server-backed, and production workflows remain pending
- **Implemented:** The direct `/studio/courses/local-course-1/review` route now renders the arena plan, sorted placement register, exact pinned revision evidence, advisory geometry warnings, exact equipment quantities, and matching human- and machine-readable specification previews without mutating local course or revision storage.
- **Review identity:** Schema `1.0.0-phase1d` canonicalizes placements and warnings into a SHA-256 course-review hash. Movement, rotation, or repinning changes the hash; source-array ordering and an unused newer revision do not. Newer revisions are shown as passive notices only.
- **Incomplete behavior:** Missing pinned product data remains visible, produces an explicit warning and missing-reference record, is excluded rather than estimated, and marks the artifact incomplete and unsuitable for production.
- **Quality evidence:** `pnpm format:check`, `pnpm lint`, `pnpm typecheck`, `pnpm test` (80 tests), and `pnpm build` pass. Real local acceptance covered mixed immutable revisions, overlap trigger/clear, stable reload and unused-revision hashes, movement/rotation hash changes, JSON parity/copy feedback, normal 3D and forced 2.5D fallback, desktop/tablet/mobile layouts, focus visibility, 44 px touch targets, print rules, 375 px containment, and empty browser warning/error logs.
- **Screenshots:** `docs/phase-1d/screenshots/course-review-desktop.png`, `course-review-mobile.png`, `course-review-placement-register.png`, `course-review-machine-readable.png`, and `course-review-newer-revision.png`.
- **Boundary:** No accounts, server persistence, cross-device sync, sharing, automatic revision upgrades, supplier approval, production release, safety/course-validity certification, pricing, checkout, payment, or ordering.
- **Next milestone outcome:** Completed in Phase 1E through the bounded explicit update-one/replace-all workflow documented in `docs/phase-1e/IMPLEMENTATION.md`; the next honest gate is visual sign-off, representative-device performance validation, and supplier confirmation rather than an automatic server or commerce phase.

## Phase 1C local course planning

### Build the first pinned-revision course boundary

- **Status:** Complete for the bounded browser-local Phase 1C prototype on 2026-07-13; authenticated/server course work remains pending
- **Implemented:** The direct `/studio/courses/local-course-1` route now restores one schema-versioned 60 × 40 m local course. Repeated obstacle placements support pointer drag, 500 mm and 2,000 mm keyboard/control movement, 15-degree rotation, stable display numbers, removal, and responsive arena-first layouts.
- **Revision pinning:** Every instance stores one exact immutable `obstacleDesignRevisionId`. Creating a changed Revision 02 leaves existing Revision 01 placements, symbols, geometry, and bill-of-material inputs unchanged. There is no silent upgrade path.
- **Planning derivations:** Rotated centre-anchored footprints drive positive-area overlap and out-of-bounds warnings. Exact pinned bills of materials drive the equipment roll-up; unresolved revisions are warned and excluded rather than estimated.
- **Quality evidence:** `pnpm format:check`, `pnpm lint`, `pnpm typecheck`, `pnpm test` (65 tests), and `pnpm build` pass. Real local acceptance covered pointer and keyboard operation, overlap and boundary trigger/clear cycles, exact mixed-revision quantities, reload restoration, Revision 01/02 pinning, normal interactive 3D, forced 2.5D fallback, desktop/tablet/mobile layout, focus visibility, 44 px touch targets, horizontal overflow, and empty browser warning/error logs.
- **Screenshots:** `docs/phase-1c/screenshots/local-course-1-desktop.png`, `local-course-1-desktop-warning.png`, `local-course-1-mobile-controls.png`, and `local-course-1-revision-pinning.png`.
- **Boundary:** This is same-browser purchase-planning geometry only. It does not add accounts, server persistence, cross-device sync, sharing, archive/delete recovery, automatic revision upgrades, safety or course-validity certification, pricing, checkout, production approval, or ordering.
- **Exact next milestone:** Phase 1D — define a bounded course review and production-spec preview over pinned placements, including an explicit design for any future update-one/replace-all action, while preserving immutable sources and continuing to exclude commerce.

## Phase 1B local draft and immutable revisions

### Add the same-device design record boundary

- **Status:** Complete for the bounded browser-local Phase 1B prototype on 2026-07-13; authenticated/server revision work remains pending
- **Implemented:** One schema-versioned mutable SPJ-04 draft now restores automatically on the same browser/device. Explicit saves append complete hash-pinned `ObstacleDesignRevision` snapshots. Saved history is never overwritten.
- **Reopen behavior:** A saved revision opens read-only and drives the exact pinned visual and six derived projections. `Duplicate to edit` creates a fresh version-1 draft identity linked to the source revision; later edits leave that source unchanged.
- **Storage safety:** Valid browser JSON round-trips exactly. Malformed, unsupported, duplicate-ID, missing-source, or hash-tampered data returns typed failures and cannot become trusted product state.
- **Typography:** Display and interface typography now uses the exact normal and italic SelfsoSans variable WOFF2 files from the Selfso frontend, with the normal style preloaded.
- **Quality evidence:** `pnpm format:check`, `pnpm lint`, `pnpm typecheck`, `pnpm test` (45 tests), and `pnpm build` pass. Real 1440 × 1000 and 375 × 812 runtime checks covered save, reload, reopen, locked controls, duplicate-to-edit, keyboard operation, interactive 3D, forced 2.5D fallback, font loading, console output, horizontal overflow, and touch target sizing.
- **Screenshots:** `docs/phase-1b/screenshots/spj-04-phase-1b-desktop.png`, `spj-04-phase-1b-desktop-revisions.png`, `spj-04-phase-1b-mobile.png`, and `spj-04-phase-1b-mobile-revisions.png`.
- **Boundary:** This is same-browser prototype persistence only. It does not add accounts, server authority, cross-device sync, sharing, archive/delete, course planning, artwork processing, ordering, payments, production confirmation, or safety claims.
- **Exact next milestone:** Phase 1C — add the first course-planning boundary using pinned saved-revision IDs, repeated placements, deterministic quantity roll-up, and non-safety geometry warnings, without commerce or silently updating placed instances.

## Phase 1A configurator foundation

### Build the first visible SPJ-04 vertical slice

- **Status:** Complete for the bounded non-sellable Phase 1A prototype on 2026-07-13; broader Phase 1 remains pending
- **Implemented:** A strict Next.js TypeScript application and direct `/studio/obstacles/spj-04` route. One normalized configuration now derives compatibility, an exact prototype bill of materials, renderer-neutral manifest, stable supplier-cost example, 5,100 × 800 mm footprint, human/machine production-spec preview, and a deterministic hash.
- **Renderer evidence:** Deterministic SVG 2.5D is present in the initial document and remains active without WebGL. Three.js and `spj-04-prototype-v1.glb` load lazily. The explicit `?force3d=fail` acceptance route preserves the current 2.5D selection and summaries.
- **Quality evidence:** `pnpm format:check`, `pnpm lint`, `pnpm typecheck`, `pnpm test` (32 tests), and `pnpm build` pass. The real local route passed desktop 1440 × 1000 and mobile 375 × 812 checks with every option, keyboard activation, no console warnings/errors, no mobile horizontal overflow, and no touch target below 44 × 44 px.
- **Screenshots:** `docs/phase-1a/screenshots/spj-04-desktop.png`, `docs/phase-1a/screenshots/spj-04-mobile.png`, `docs/phase-1a/screenshots/spj-04-desktop-red-gate.png`, and `docs/phase-1a/screenshots/spj-04-3d-fallback.png`.
- **Boundary:** This does not complete accounts, persistence, saved revisions, sharing, course planning, artwork upload/scanning, ordering, payments, production confirmation, or the full Phase 1 plan.
- **Exact next milestone:** Phase 1B — add the versioned obstacle-design draft and immutable saved-revision boundary, including anonymous same-device restoration and reopen/duplicate behavior, while keeping this derivation contract and both render adapters unchanged and still excluding course planning and commerce.

## Required before vertical-slice visual sign-off

### Produce a real 3D and 2.5D asset benchmark

- **Status:** Benchmark integrated into the Phase 1A local runtime with desktop/mobile renderer-parity checks; Tim's visual sign-off and a named representative-device load check remain pending
- **What:** Produce one representative modular obstacle as a true 3D asset plus deterministic front, angle, and detail fallback renders of the same validated configuration.
- **Why:** The approved mockups establish the target experience, but they do not prove that supplier geometry, materials, component seams, load time, and the fallback can meet that visual standard.
- **Pros:** Tests the highest-risk visual dependency early; validates the asset pipeline; prevents the configurator from being built around unrealistic imagery; gives engineering a real performance target.
- **Cons:** Requires asset-production effort before the broader interface is implemented and may expose missing supplier dimensions or reference photography.
- **Context:** The product promises Nike By You-like immediate customization. One shared configuration must drive the 3D model, 2.5D fallback, bill of materials, price, course symbol, and production specification. The benchmark should use the approved Graphic Color Studio visual direction and prove that switching renderer modes never changes the selected product.
- **Prototype evidence:** `assets/spj-04/prototype-v1/` contains the pinned configuration, GLB, six deterministic views, comparison board, bill of materials, footprint, manifest, and benchmark report. It passes the 3 MB GLB, 150,000-triangle, and 2,048-pixel texture budgets.
- **Depends on / blocked by:** The prototype uses the separately documented inferred SPJ-04 profile. Production-grade sign-off still requires confirmed supplier dimensions, component matrix, color/material references, and print-area specification. Final visual sign-off also needs Tim's review and a representative-device load check.
- **Acceptance evidence:** Approved visual comparison between 3D and 2.5D; verified component combinations; measured file/model budgets; representative-device load result; documented asset naming and versioning convention.

## Required before sellable commerce implementation

### Run the Phase 2 sellable-commerce engineering review

- **Status:** Pending
- **What:** Review and lock the architecture for deposit checkout, approved artwork, immutable quotes, provider webhooks, balance collection, cancellation, refunds, shipment holds, customer communication, and the minimum staff order console.
- **Why:** These paths move money and release production work. Implementing them from the current design assumptions could create duplicate charges, incorrect tax or delivery totals, unrecoverable order states, or factory work based on an unapproved specification.
- **Pros:** Creates a hard engineering gate before irreversible financial and operational side effects; preserves the reasoning that Phase 1 deliberately excludes payment; gives tests and state transitions confirmed commercial inputs.
- **Cons:** Requires a second engineering review after Phase 0 and cannot be completed until external business decisions are signed off.
- **Context:** The 2026-07-13 Phase 0–1 engineering review deliberately limits implementation to the non-sellable vertical slice: shared configuration truth, renderer parity, saved revisions, course placement, sharing, and production-spec preview. Phase 2 must reuse those pinned revisions and typed domain results, but its quote, payment, balance, refund, and shipment transitions need a separate failure-mode and test review against the chosen provider and approved retail policy.
- **Effort:** L
- **Priority:** P0 before sellable release; not blocking Phase 1
- **Depends on / blocked by:** Phase 0 commercial sign-off; confirmed launch country and value-added-tax treatment; delivered-price formula; supplier production contract; deposit, balance, cancellation, refund, warranty, and custom-return policies; payment-provider selection.
- **Acceptance evidence:** Approved Phase 2 architecture and state diagrams; provider-specific idempotency and webhook contract; immutable quote/order schema; failure-mode table; full unit, integration, contract, and end-to-end test plan; deployment and reconciliation runbook.
